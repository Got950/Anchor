import type { Metadata } from "next";

export const metadata: Metadata = { title: "BlockForge style pack — Craftify" };

export default function GalleryLayout({ children }: { children: React.ReactNode }) {
  return children;
}
